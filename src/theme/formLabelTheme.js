import { defineStyleConfig } from "@chakra-ui/react";

export const formLabelTheme = defineStyleConfig({
  baseStyle: {
    fontFamily: "Karla",
    fontSize: "18px",
    fontWeight: 500,
    color: "highlight.100",
  },
});
